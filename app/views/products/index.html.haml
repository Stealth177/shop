%h1
  Список товаров
  %table
    - @products.each do |product|
      %tr{:class => cycle('list_line_odd', 'list_line_even')}
        %td
          = image_tag product.photo.url(:thumb)
        %td.list_description
          %dl
            %dt= product.title
            %dd=raw truncate(strip_tags(product.description) , |
                length: 80)                                |
        %td.list_actions
          = link_to 'Просмотр', product
          %br/
          = link_to 'Редактирование' , edit_product_path(product)
          %br/
          = link_to 'Удаление', product, |
            confirm: 'Вы уверены?',   |
            method: :delete             |